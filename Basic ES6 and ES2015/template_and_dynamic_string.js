const templat_string=`this is a template string ,here we
write multi line string wihout error faceing
,normally when we write any multi line string, we use + sign for between 2 string `

console.log(templat_string);
const price=100
const quantity=3
const dynamicString=`this is item Price is ${price}, and we need ${quantity} item ,and 
total price is ${price*quantity} Dollar `

console.log(dynamicString);